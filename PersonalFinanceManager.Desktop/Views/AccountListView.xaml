<UserControl x:Class="PersonalFinanceManager.Desktop.Views.AccountListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Accounts" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Content="+ New Account" Command="{Binding ShowCreateFormCommand}"
                    Style="{StaticResource PrimaryButton}" Margin="16,0,0,0" Height="32"/>
        </StackPanel>

        <!-- Error -->
        <TextBlock DockPanel.Dock="Top" Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource DangerBrush}" FontSize="12" Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisConverter}}"/>

        <!-- Create form -->
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,16"
                Visibility="{Binding ShowCreateForm, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="New Account" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition/>
                        <ColumnDefinition Width="16"/><ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Name" FontSize="12" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormInput}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Type" FontSize="12" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding AccountTypes}"
                                  SelectedItem="{Binding NewType}" Padding="8,6" FontSize="13"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Initial Balance" FontSize="12" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewInitialBalance, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormInput}"/>
                    </StackPanel>
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Create" Command="{Binding CreateCommand}"
                            Style="{StaticResource PrimaryButton}" Width="80" Height="32"/>
                    <Button Content="Cancel" Command="{Binding CancelCreateCommand}"
                            Style="{StaticResource SecondaryButton}" Width="80" Height="32" Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Accounts grid -->
        <DataGrid ItemsSource="{Binding Accounts}"
                  SelectedItem="{Binding SelectedAccount}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  CanUserAddRows="False" RowHeight="36" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="80"/>
                <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
                <DataGridTemplateColumn Header="Actions" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Toggle"
                                        Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource SecondaryButton}" Height="24" Padding="8,2" FontSize="11"/>
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource DangerButton}" Height="24" Padding="8,2" FontSize="11" Margin="4,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
